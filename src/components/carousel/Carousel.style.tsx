import { css } from '@emotion/react';

export const CarouselStyle = {
  root: css({
    display: 'flex',
    flexDirection: 'row',
  }),
  content: css({
    display: 'flex',
    flexDirection: 'row',
    padding: '0',
    gap: '1.25rem',
  }),
};
