import { css } from '@emotion/react';

export const SkillsStyle = {
  root: css({
    display: 'flex',
    height: '100%',
    width: '100%',
  }),
  skillGrid: css({
    display: 'flex',
    gap: '10px',
    flexWrap: 'wrap',
  }),
};
