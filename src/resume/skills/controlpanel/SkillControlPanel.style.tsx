import { css } from '@emotion/react';

export const SkillControlPanelStyle = {
  root: css({
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    width: '100%',
    height: '100%',
  }),
};
