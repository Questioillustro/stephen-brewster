import { css } from '@emotion/react';

export const EducationTileStyle = {
  root: css({}),
  card: css({
    display: 'flex',
    flexDirection: 'column',
    minHeight: '120px',
    minWidth: '200px',
    padding: '10px 30px',
  }),
};
