import { css } from '@emotion/react';

export const EducationTileStyle = {
  root: css({}),
  card: css({
    display: 'flex',
    flexDirection: 'column',
    minHeight: '120px',
    minWidth: '380px',
    padding: '10px 30px',
  }),
};
