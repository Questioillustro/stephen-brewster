import { css } from '@emotion/react';

export const BoulderingStyle = {
  root: css({
    textAlign: 'left',
  }),
  myGyms: css({
    alignItems: 'center',
    display: 'flex',
    flexDirection: 'row',
    marginTop: '10px',
    gap: '10px',
  }),
};
