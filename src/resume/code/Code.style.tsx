import { css } from '@emotion/react';

export const CodeStyle = {
  root: css({
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    gap: '50px',
  }),
  codeLink: css({
    display: 'flex',
    flexDirection: 'column',
  }),
};
