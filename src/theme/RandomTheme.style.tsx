import { css } from '@emotion/react';

export const RandomThemeStyle = {
  root: css({
    display: 'flex',
    height: '100%',
    flexDirection: 'row',
    alignItems: 'center',
    cursor: 'pointer',
    gap: '.3125rem',
  }),
};
