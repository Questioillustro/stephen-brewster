import { css } from '@emotion/react';

export const HeaderStyle = {
  root: css({ textAlign: 'center' }),
  links: css({
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
    gap: '10px',
    marginTop: '10px',
  }),
};
