import { css } from '@emotion/react';

export const ControlPanelStyle = {
  root: css({
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-evenly',
  }),
  darkThemeToggle: css({}),
  randomThemeToggle: css({}),
};
