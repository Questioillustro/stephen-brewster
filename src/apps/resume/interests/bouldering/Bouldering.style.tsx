import { css } from '@emotion/react';

export const BoulderingStyle = {
  root: css({
    textAlign: 'left',
  }),
  myGyms: css({
    alignItems: 'center',
    display: 'flex',
    flexDirection: 'row',
    marginTop: '.625rem',
    gap: '.625rem',
  }),
};
